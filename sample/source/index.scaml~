%html
  %head
    %title
    %link(href={uri("css/default.css")} rel="stylesheet" type="text/css")
  %body
    - for (resource <- allResources.filter(_.tags.contain("blog")).sortBy(_.pubDate))
    .post
      .body
        - unescape(body)
      .tags
        - for (tag <- tags)
          %span.tag= tag
    